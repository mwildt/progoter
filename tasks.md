# Tasks

## open
* während eine chat-context completion läuft, möchte ich einen butten in der ui, mit dem ich die aktion stoppen kann.
  neuer Endpunkt /chat/context-id/cancel. Der aktuelle state des ChatContext muss dafür im MOdell gehlaten werden. Diese Info muss auch live in den client geschickt werden (streaming-even) 

## done